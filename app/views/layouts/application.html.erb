<!DOCTYPE html>
<html>
  <head>
    <title>EventListener</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "homepage", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  </head>

  <body>
  <!--
    <% if user_signed_in? %>
      <div class="logout">
        [<%= link_to 'Logout', destroy_user_session_path, :method => :delete%>]
      </div>
    <% end %>

    <header>
        <div class="titolo">
            <h1>EventListener</h1>
        </div>

        <div class="nome">
            <h1>Utente: <%= @user_name %></h1>
        </div>
    </header>

    <div class="grid-container" id="colonne">
        <div class="grid-item">Fan di:</div>
        <div class="grid-item">Raduni:</div>
        <div class="grid-container" id="righe">
            <div class="grid-item">Raduni a cui partecipi:</div>
            <div class="grid-item">Raduni creati:</div>
        </div>
    </div>
    -->

    <%= yield %>

  </body>
</html>
